#ifndef SOURCE_H
#define SOURCE_H
#include "preferences.h"
#include <string.h>

void scaner(struct preferences* array, const unsigned int size);
void analizer(struct preferences* array, const unsigned int size, const unsigned int user);
unsigned int Foo (unsigned int x, unsigned int y, unsigned int z,unsigned int x0, unsigned int y0, unsigned int z0);
void printInfoUser(struct preferences* array, unsigned int position);
#endif // SOURCE_H
